# usage-trace Codex plugin

This is a thin Codex plugin wrapper used by the repo marketplace at
`.agents/plugins/marketplace.json`. It keeps the installable plugin under
`plugins/usage-trace/` for Codex marketplace installation.
